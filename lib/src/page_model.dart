import 'dart:ui';

class PageModel {
  final Color color;
  final String imageAssetPath;
  final String title;
  final String body;

  PageModel(this.color, this.imageAssetPath, this.title, this.body);
}
